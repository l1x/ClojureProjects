(ns die-harder.core)

(def jugs [0 0])
(def caps [5 3])

(defn sum [xs])

(defn pour [from to]
  (let [poured (min ())])
  )

(defn foo
  "I don't do a whole lot."
  [x]
  (println x "Hello, World!"))
